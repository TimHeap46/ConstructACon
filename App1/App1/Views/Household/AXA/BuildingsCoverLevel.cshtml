@model App1.Models.HouseholdViewModel

@if (Model.CoverAmount > 0 && Model.CoverAmount < 500000)
{
    { <text>1</text> }
}
else if (Model.CoverAmount > 500000 && Model.CoverAmount < 1000000)
{
    { <text>2</text> }
}
else
{
    { <text>FILTER</text> }
}